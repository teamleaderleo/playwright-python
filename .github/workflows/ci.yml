name: CI

on:
  pull_request:
    branches:
      - release-*

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

jobs:
  superseded:
    name: Superseded carrier
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
    - run: echo superseded
